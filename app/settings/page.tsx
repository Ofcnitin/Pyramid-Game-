'use client'; import {useState} from 'react'; import AppShell from '@/components/AppShell'; export default function Settings(){const [saved,setSaved]=useState(false);return <AppShell><div className="topbar"><div><h1 className="page-title">Settings</h1><div className="sub">Manage your account and game preferences.</div></div></div><div className="settings-grid"><div className="card setting"><h3 className="serif" style={{fontSize:21}}>Account</h3><div className="field"><label>Display name</label><input defaultValue="Nick" maxLength={30}/></div><div className="field"><label>Email</label><input defaultValue="nick@example.com" type="email" disabled/></div><button className="btn red" onClick={()=>setSaved(true)}>Save changes</button>{saved&&<div className="success" style={{marginTop:10}}>Changes saved.</div>}</div><div className="card setting"><h3 className="serif" style={{fontSize:21}}>Security</h3><p className="sub">Your account is required for all gameplay. Room passwords are separate credentials and are never shown after creation.</p><button className="btn light">Change password</button></div></div></AppShell>}

