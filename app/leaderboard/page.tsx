import AppShell from '@/components/AppShell'; import {players} from '@/lib/mock'; export default function Page(){return <AppShell><div className="topbar"><div><h1 className="page-title">Leaderboard</h1><div className="sub">See where perception has placed everyone.</div></div><div className="seg"><button className="active">Global</button><button>Friends</button></div></div><div className="card" style={{padding:'10px 20px'}}><table className="table"><thead><tr><th>#</th><th>Player</th><th>Points</th><th>Change</th></tr></thead><tbody>{players.map(p=><tr key={p.id}><td>{p.rank}</td><td><span style={{display:'inline-flex',alignItems:'center',gap:9}}><span className="avatar" style={{width:28,height:28,fontSize:10}}>{p.name[0]}</span>{p.name}</span></td><td>{p.points.toLocaleString()}</td><td className={p.change>0?'rank-up':p.change<0?'rank-down':''}>{p.change>0?'↑ '+p.change:p.change<0?'↓ '+Math.abs(p.change):'—'}</td></tr>)}</tbody></table></div></AppShell>}
