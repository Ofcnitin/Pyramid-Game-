'use client'; import {useState} from 'react'; import AppShell from '@/components/AppShell'; import {Edit3} from 'lucide-react'; export default function Profile(){const [tab,setTab]=useState('History');return <AppShell><div className="topbar"><div><h1 className="page-title">Profile</h1><div className="sub">Your place in the hierarchy, recorded.</div></div></div><div className="card profile-head"><div className="profile-avatar"/><div><div className="profile-name">Nick</div><div className="small">#482 · “Just another player.”</div></div><div className="profile-stat"><span className="small">Points</span><strong>1,240</strong></div><div className="profile-stat"><span className="small">Current Round</span><strong>3</strong></div><div className="profile-stat"><span className="small">Current Rank</span><strong>#482</strong></div></div><div className="card" style={{marginTop:16,padding:'0 18px'}}><div className="tabs">{['Stats','History','Achievements'].map(x=><button key={x} className={tab===x?'active':''} onClick={()=>setTab(x)}>{x}</button>)}</div>{tab==='History'?<div style={{padding:'10px 0'}}>{[['Round 3','Voted','2 hours ago'],['Round 2','Rank moved from #503 → #482','1 day ago'],['Round 1','Joined the game','2 days ago']].map(x=><div key={x[0]} style={{display:'grid',gridTemplateColumns:'100px 1fr 110px',padding:'16px 4px',borderBottom:'1px solid #eee'}}><b>{x[0]}</b><span>{x[1]}</span><span className="small">{x[2]}</span></div>)}</div>:<div className="empty">{tab} will appear here as you play.</div>}</div></AppShell>}
